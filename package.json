{
  "name": "js-ai-promote",
  "version": "1.0.0",
  "description": "JS AI Promote Project",
  "scripts": {
    "start": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "install-all": "npm install && cd frontend && npm install",
    "dev": "concurrently \"cd frontend && npm start\" \"cd backend && npm start\"",
    "verify": "node docs/verify-setup.js",
    "check-deps": "node docs/check-dependencies.js"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
} 