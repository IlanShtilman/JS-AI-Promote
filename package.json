{
  "name": "js-ai-promote",
  "version": "1.0.0",
  "description": "JS AI Promote Project",
  "scripts": {
    "start": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "install-all": "npm install && cd frontend && npm install",
    "dev": "concurrently --names \"Frontend,Backend\" --prefix-colors \"blue,green\" \"cd frontend && npm start\" \"cd backend && mvnw.cmd spring-boot:run\"",
    "verify": "node frontend/verify-setup.js",
    "check-deps": "node frontend/check-dependencies.js"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
} 